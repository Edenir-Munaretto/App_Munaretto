fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
supabase>=1.0.0
python-dotenv>=1.0.0
docxtpl>=0.16.7
openpyxl>=3.1.0
fpdf2>=2.7.0
python-multipart>=0.0.6
num2words
PyJWT>=2.8.0
# Necessário para conversão de DOCX→PDF no Windows via Word COM (fallback)
# Requer Microsoft Word instalado. Instale com: pip install pywin32
# pywin32>=306

# Testes
pytest>=7.0.0
httpx>=0.24.0

fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
email-validator>=2.0.0
supabase>=1.0.0
python-dotenv>=1.0.0
docxtpl>=0.16.7
openpyxl>=3.1.0
fpdf2>=2.7.0
python-multipart>=0.0.6
num2words
PyJWT>=2.8.0
# Armazenamento de arquivos no Backblaze B2 (protocolo compatível com S3)
boto3>=1.34.0
# Conversão DOCX→PDF no Windows (requer Microsoft Word instalado)
# pywin32 é Windows-only; no Linux (Docker/Render) o LibreOffice faz a conversão
docx2pdf>=0.1.8; platform_system == "Windows"
pywin32>=306; platform_system == "Windows"

# Testes
pytest>=7.0.0
httpx>=0.24.0

# Qualidade de código (linter + formatador)
ruff>=0.16.0
